<div class='ui stackable three column grid'>
  <div class='column'></div>
  <div class='column'>
    <div class='ui card' style='width: 100%'>
      <div class='content'>
        <span class='header'>{{ forename }} {{ surname }}</span>
        <div class='meta'>
          <span class='date'>@{{ username }}</span>
        </div>
      </div>
    </div>
    <h3 class='ui dividing header'>Polls Created</h3>
    {{#polls_created}}
      <div class='ui card poll' style='width: 100%' onclick='visit("/poll/{{ id }}")'>
        <div class='content'>
          <span class='header'>{{ name }}</span>
          <span class='meta'>{{ description }}</span>
        </div>
      </div>
    {{/polls_created}}
    <h3 class='ui dividing header'>Activity</h3>
    <div class='ui feed'>
    {{#activities}}
      <div class='event'>
        <div class='content'>
          <i class='ui icon {{#is_private}}lock{{/is_private}} {{^is_private}}globe{{/is_private}}'></i>
          <a href='#/user/{{ user.username }}'>{{ user.forename }} {{ user.surname }}</a>
          {{ text }}
          <a href='#/poll/{{ poll.id }}'>{{ poll.name }}</a>
        </div>
      </div>
    {{/activities}}
  </div>
  <div class='column'></div>
</div>
