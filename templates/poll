<div id='poll'>
  {{#editable}}
  <div id='poll-panel'>
    <a href='#/edit-poll/{{ id }}'>edit</a>
    {{^finalized}}<a href='#/finalize-poll/{{ id }}'>finalize</a>{{/finalized}}
    <a href='#/delete-poll/{{ id }}'>delete</a>
  </div>
  {{/editable}}

  <h2 class='poll-title'>{{ name }}</h2>
  <span class='meta'>{{ user.forename }} {{ user.surname }} (<a href='#/user/{{user.username}}'>@{{ user.username }}</a>)</span>
  <p>{{ description }}</p>

  <hr />
  <div id='votes'>
  {{^finalized}}
    {{#votes}}
      <div class='vote'>
        {{^voted}}
          <span class='poll-option not-voted'>{{ name }} ({{ num }})</span>
          <a class='add-vote' data-id='{{ id }}'>vote</a>
        {{/voted}}
        {{#voted}}
          <span class='poll-option voted'>{{ name }} ({{ num }})</span>
          <a class='add-unvote' data-id='{{ id }}'>unvote</a>
        {{/voted}}
      </div>
    {{/votes}}
    {{^votes}}<span class='gray'>no vote options</span>{{/votes}}
  {{/finalized}}
  {{#finalized}}
    {{#votes}}
      <div class='vote finalized'>
        <span class='poll-option{{^voted}} not-voted{{/voted}}{{#voted}} voted{{/voted}}'>{{ name }} ({{ num }})</span>
      </div>
    {{/votes}}
    {{^votes}}<span class='gray'>no vote options</span>{{/votes}}
  {{/finalized}}
  </div>

  <hr />
  {{#finalized}}
  <p id='poll-finalized'>Poll finalized.</p>
  <hr />
  {{/finalized}}
  <form id='comment-box'>
      <textarea name='text'/>
      <button class='comment-box-submit'>Reply</button>
  </form>

  <div id='comments'></div>
</div>
